name: Setup tmux + Codex (DataAnnotation Qualification)

on:
  workflow_dispatch:  # Run manually
  push:
    branches: [ main, master ]

jobs:
  setup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install tmux
        run: |
          sudo apt-get update
          sudo apt-get install -y tmux

      - name: Configure tmux (history + mouse)
        run: |
          cat > ~/.tmux.conf << EOF
          set-option -g history-limit 100000
          set -g mouse on
          EOF
          echo "✅ tmux config created:"
          cat ~/.tmux.conf

      - name: Install Node.js (for Codex)
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Codex CLI
        run: |
          npm install -g @openai/codex
          echo "✅ Codex version:"
          codex --version

      - name: Create Codex config with proxy (for DataAnnotation)
        run: |
          mkdir -p ~/.codex
          cat > ~/.codex/config.toml << EOF
          openai_base_url = "https://app.dataannotation.tech/api/llm_proxy/openai/v1"
          EOF
          echo "✅ Codex proxy config created"
          cat ~/.codex/config.toml

      - name: Verification
        run: |
          echo "=== Verification ==="
          echo "tmux version:"
          tmux -V
          echo "Codex version:"
          codex --version
          echo "tmux config:"
          cat ~/.tmux.conf
          echo "Setup completed successfully!"

      - name: Final Instructions
        run: |
          echo ""
          echo "=================================================================="
          echo "✅ Setup Complete!"
          echo "Next steps for qualification:"
          echo "1. Set environment variable: export OPENAI_API_KEY=Q9A4AXWDH3V7K6GWMGWQ7TNW"
          echo "2. Run: tmux"
          echo "3. Inside tmux, run: codex"
          echo "=================================================================="
